<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.slppp.app.modular.system.dao.MemberMapper">

	<!-- 通用查询映射结果 -->
	<resultMap id="BaseResultMap" type="com.slppp.app.modular.system.model.Member">
		<id property="id" column="id" jdbcType="INTEGER"/>
		<result column="username" property="username" jdbcType="VARCHAR"/>
		<result column="password" property="password" jdbcType="VARCHAR"/>
		<result column="email" property="email" jdbcType="VARCHAR"/>
		<result column="private_key" property="privateKey" jdbcType="VARCHAR"/>
	</resultMap>

	<sql id="Base_Column_List">
		id, username, password, email, private_key
	</sql>

	<insert id="insert" parameterType="com.slppp.app.modular.system.model.Member">
		insert into member
		<trim prefix="(" suffix=")" suffixOverrides="," >
			<if test="username != null" >
				`username`,
			</if>
			<if test="password != null" >
				password,
			</if>
			<if test="email != null" >
				email,
			</if>
			<if test="privateKey != null" >
				private_key,
			</if>
		</trim>
		<trim prefix="values (" suffix=")" suffixOverrides="," >
			<if test="username != null" >
				#{username,jdbcType=VARCHAR},
			</if>
			<if test="password != null" >
				#{password,jdbcType=VARCHAR},
			</if>
			<if test="email != null" >
				#{email,jdbcType=VARCHAR},
			</if>
			<if test="privateKey != null" >
				#{privateKey,jdbcType=INTEGER},
			</if>
		</trim>
	</insert>

	<select id="findByUserName" resultMap="BaseResultMap">
		SELECT
		<include refid="Base_Column_List" />
		FROM `member` where username = #{username}
	</select>

	<select id="findByUserNameAndPassword" resultMap="BaseResultMap">
		SELECT
		<include refid="Base_Column_List" />
		FROM `member` where username = #{username} and password = #{password}
	</select>

</mapper>
